#This file was generated by ChatGPT upon request
get:
  summary: "Get filtered posts by date range"
  description: "Returns posts filtered by start and end date with optional pagination and category filtering."
  tags:
    - posts
  parameters:
    - in: query
      name: start
      schema:
        type: string
        format: date
      required: true
      example: "2025-09-01"
      description: "Start date (YYYY-MM-DD format)"
    - in: query
      name: end
      schema:
        type: string
        format: date
      required: true
      example: "2025-10-01"
      description: "End date (YYYY-MM-DD format)"
    - in: query
      name: offset
      schema:
        type: integer
        minimum: 0
      required: false
      example: 0
      description: "Offset for pagination"
    - in: query
      name: limit
      schema:
        type: integer
        minimum: 1
        maximum: 200
      required: false
      example: 50
      description: "Limit for pagination (max 200)"
    - in: query
      name: cid
      schema:
        type: integer
      required: false
      example: 1
      description: "Category ID filter (-1 for all categories)"
  responses:
    "200":
      description: "Successful response with filtered posts"
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: true
              posts:
                type: array
                items:
                  type: object
                  properties:
                    pid:
                      type: integer
                    tid:
                      type: integer
                    content:
                      type: string
                    timestamp:
                      type: integer
                    timestampISO:
                      type: string
                    user:
                      type: object
                    topic:
                      type: object
                    category:
                      type: object
            required:
              - success
              - posts
    "400":
      description: "Bad request - missing or invalid parameters"
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              error:
                type: string
                example: "missing_start_or_end"
    "500":
      description: "Internal server error"
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              error:
                type: string